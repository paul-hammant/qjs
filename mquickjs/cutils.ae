exports (p_max, p_min, p_clamp)
p_max(a: int, b: int) -> int { if (a > b) { return a; } return b; }
p_min(a: int, b: int) -> int { if (a < b) { return a; } return b; }
p_clamp(v: int, lo: int, hi: int) -> int { if (v < lo) { return lo; } if (v > hi) { return hi; } return v; }
