// Test: arrow closure with return value and trailing block

main() {
    // Arrow closure with typed params
    doubler = |x: int| -> x * 2

    // Trailing block on a function call
    println("trailing block test")
}
