// Test: closure capturing variables from enclosing scope

main() {
    multiplier = 3

    // Closure that captures 'multiplier'
    scale = |x: int| -> x * multiplier

    println("capture closure parsed ok")
}
