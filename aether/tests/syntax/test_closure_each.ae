// Test: closure with captures that does real computation

main() {
    base = 100
    factor = 3

    // Closure capturing 'base' and 'factor'
    compute = |x: int| -> base + x * factor

    println("closure with captures ok")
}
